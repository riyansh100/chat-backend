port 6381
bind 127.0.0.1

# replicate from primary
replicaof 127.0.0.1 6380

# replica is read-only by default — keep it that way
replica-read-only yes

# persistence (separate file from primary)
dbfilename dump-replica.rdb
dir ./

# logging
loglevel notice
logfile "redis-replica.log"